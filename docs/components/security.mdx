---
title: "Security Components"
description: "Open source security tools for security automation workflows"
---

Security components wrap popular open-source tools for subdomain discovery, DNS resolution, port scanning, secret detection, vulnerability analysis, threat intelligence, and more. Sentris Flow ships with **28 security components**.

---

## Advanced: container resources

Docker-based security components run with tiered default limits (light / standard / heavy). On any Docker security step, enable **Override container resources** in the config panel to set custom `Container memory limit` (e.g. `4g`, `512m`) and/or `Container CPU limit` (e.g. `2`, `0.5`) for that node only. Leave the toggle off to keep defaults.

- Host Docker must have enough RAM/CPU for overrides; OOM kills typically show exit code **137**.
- Worker caps: `SENTRIS_DOCKER_MAX_MEMORY` (default `8g`) and `SENTRIS_DOCKER_MAX_CPUS` (default `4`) in [`worker/.env`](../../worker/.env.example).

---

## Subdomain Discovery

### Subfinder

<Info>
[GitHub](https://github.com/projectdiscovery/subfinder) · Docker: `ghcr.io/zebbern/subfinder`
</Info>

Discovers subdomains using passive sources.

| Input | Type | Description |
|-------|------|-------------|
| `domains` | Array | Target domains to enumerate |
| `providerConfig` | Secret | Optional provider-config.yaml for authenticated sources |

| Output | Type | Description |
|--------|------|-------------|
| `subdomains` | Array | Discovered subdomain hostnames |
| `rawOutput` | String | Raw tool output |
| `subdomainCount` | Number | Total subdomains found |

**Example command:** `subfinder -d example.com -silent`

---

### Amass

<Info>
[GitHub](https://github.com/owasp-amass/amass) · Docker: `ghcr.io/zebbern/amass`
</Info>

Active and passive subdomain enumeration.

| Input | Type | Description |
|-------|------|-------------|
| `domains` | Array | Target domains |
| `mode` | Select | `passive` or `active` |

| Parameter | Type | Description |
|-----------|------|-------------|
| `timeout` | Number | Timeout in minutes |
| `maxDns` | Number | Max DNS queries per second |

---

### ShuffleDNS + MassDNS

<Info>
[GitHub (ShuffleDNS)](https://github.com/projectdiscovery/shuffledns) · [GitHub (MassDNS)](https://github.com/blechschmidt/massdns) · Docker: `ghcr.io/zebbern/shuffledns-massdns`
</Info>

High-performance DNS bruteforcing and resolution. This is a combined image that has both ShuffleDNS with MassDNS pre-installed. 

| Input | Type | Description |
|-------|------|-------------|
| `domains` | Array | Target domains |
| `wordlist` | File | Wordlist for bruteforcing |
| `resolvers` | Array | Custom DNS resolvers |

---

## DNS Resolution

### DNSX

<Info>
[GitHub](https://github.com/projectdiscovery/dnsx) · Docker: `ghcr.io/zebbern/dnsx`
</Info>

Resolves DNS records with support for multiple record types and custom resolvers.

| Input | Type | Description |
|-------|------|-------------|
| `domains` | Array | Domains to resolve |
| `recordTypes` | Array | DNS types: A, AAAA, CNAME, MX, NS, TXT, etc. |
| `resolvers` | Array | Custom resolver IPs (e.g., 1.1.1.1:53) |

| Parameter | Type | Description |
|-----------|------|-------------|
| `threads` | Number | Concurrent workers (default: 100) |
| `retryCount` | Number | Retry attempts (default: 2) |
| `rateLimit` | Number | Requests per second |
| `showCdn` | Boolean | Annotate CDN providers |
| `showAsn` | Boolean | Include ASN info |

| Output | Type | Description |
|--------|------|-------------|
| `results` | Array | DNS responses grouped by record type |
| `resolvedHosts` | Array | Unique resolved hostnames |
| `rawOutput` | String | Raw JSONL output |

---

## HTTP Probing

### httpx

<Info>
[GitHub](https://github.com/projectdiscovery/httpx) · Docker: `ghcr.io/zebbern/httpx`
</Info>

Probes hosts for live HTTP services and captures response metadata.

| Input | Type | Description |
|-------|------|-------------|
| `targets` | Array | Hostnames or URLs to probe |

| Parameter | Type | Description |
|-----------|------|-------------|
| `ports` | String | Comma-separated ports (e.g., "80,443,8080") |
| `statusCodes` | String | Filter by HTTP status codes |
| `threads` | Number | Concurrency level |
| `followRedirects` | Boolean | Follow HTTP redirects |
| `tlsProbe` | Boolean | Probe TLS endpoints |
| `path` | String | Specific path to probe |

| Output | Type | Description |
|--------|------|-------------|
| `results` | Array | HTTP response metadata |
| `rawOutput` | String | Raw httpx JSON lines |

**Example command:** `httpx -l targets.txt -json -status-code 200,301`

---

## Port Scanning

### Naabu

<Info>
[GitHub](https://github.com/projectdiscovery/naabu) · Docker: `ghcr.io/zebbern/naabu`
</Info>

Fast active port scanning using SYN/CONNECT probes.

| Input | Type | Description |
|-------|------|-------------|
| `targets` | Array | Hostnames or IPs to scan |

| Parameter | Type | Description |
|-----------|------|-------------|
| `ports` | String | Custom ports (e.g., "80,443,1000-2000") |
| `topPorts` | Number | Scan top N common ports |
| `rate` | Number | Packets per second |
| `retries` | Number | Retry attempts per port |
| `enablePing` | Boolean | Ping probe before scanning |

| Output | Type | Description |
|--------|------|-------------|
| `findings` | Array | Open ports per target |
| `openPortCount` | Number | Total open ports found |

**Example command:** `naabu -host scanme.sh -top-ports 100`

---

## Vulnerability Scanning

### Nuclei

<Info>
[GitHub](https://github.com/zebbern/Sentris) · Docker: `ghcr.io/zebbern/nuclei`
</Info>

Template-based vulnerability scanning. This is nuclei custom image with nuclei-templates baked in.

| Input | Type | Description |
|-------|------|-------------|
| `targets` | Array | URLs or hosts to scan |
| `templates` | Array | Template IDs or paths |

| Parameter | Type | Description |
|-----------|------|-------------|
| `severity` | Array | Filter by severity (critical, high, medium, low) |
| `rate` | Number | Requests per second |
| `concurrency` | Number | Parallel template executions |

| Output | Type | Description |
|--------|------|-------------|
| `findings` | Array | Detected vulnerabilities |
| `criticalCount` | Number | Critical findings count |

---

## Secret Detection

### TruffleHog

<Info>
[GitHub](https://github.com/trufflesecurity/trufflehog) · Docker: `ghcr.io/zebbern/trufflehog`
</Info>

Scans for leaked credentials across repositories, filesystems, and cloud storage.

| Input | Type | Description |
|-------|------|-------------|
| `scanTarget` | String | Repository URL, path, bucket, or image |
| `scanType` | Select | git, github, gitlab, s3, filesystem, docker |

| Parameter | Type | Description |
|-----------|------|-------------|
| `onlyVerified` | Boolean | Show only verified secrets (default: true) |
| `branch` | String | Specific branch to scan |
| `sinceCommit` | String | Scan commits since reference (for PR scanning) |

| Output | Type | Description |
|--------|------|-------------|
| `secrets` | Array | Detected secrets with verification status |
| `verifiedCount` | Number | Number of verified secrets |
| `hasVerifiedSecrets` | Boolean | Alert flag |

**Example command:** `trufflehog git https://github.com/org/repo --results=verified --json`

---

## Cloud Security

### Prowler Scan

<Info>
[GitHub](https://github.com/prowler-cloud/prowler) · Docker: `ghcr.io/zebbern/prowler`
</Info>

Cloud (AWS, Azure, GCP) security posture management. Best practices auditing.

| Input | Type | Description |
|-------|------|-------------|
| `credentials` | Object | AWS credentials |
| `checks` | Array | Specific checks to run |

| Parameter | Type | Description |
|-----------|------|-------------|
| `severity` | Array | Filter by severity |
| `services` | Array | AWS services to audit |

### Supabase Scanner

<Info>
[GitHub](https://ghcr.io/zebbern/supabase-scanner) · Docker: `ghcr.io/zebbern/supabase-scanner`
</Info>

Scans Supabase instances for misconfigurations.

| Input | Type | Description |
|-------|------|-------------|
| `projectUrl` | String | Supabase project URL |
| `apiKey` | Secret | Supabase API key |

---

### Checkov

<Info>
[GitHub](https://github.com/bridgecrewio/checkov) · Docker: `bridgecrew/checkov`
</Info>

Static analysis for Infrastructure-as-Code (Terraform, CloudFormation, Kubernetes, Dockerfile, Helm).

| Input | Type | Description |
|-------|------|-------------|
| `target` | String | IaC file content to scan |
| `customFlags` | String | Additional CLI flags |

| Parameter | Type | Description |
|-----------|------|-------------|
| `framework` | Select | IaC framework (terraform, cloudformation, kubernetes, dockerfile, helm, serverless) |

---

## Web Security

### Ffuf

<Info>
[GitHub](https://github.com/ffuf/ffuf) · Docker: `ffuf/ffuf`
</Info>

Web fuzzer for directory, file, and parameter discovery.

| Input | Type | Description |
|-------|------|-------------|
| `target` | String | URL with FUZZ keyword for fuzzing |
| `wordlist` | String | Newline-separated wordlist content |
| `customFlags` | String | Additional CLI flags |

---

### Katana

<Info>
[GitHub](https://github.com/projectdiscovery/katana) · Docker: `projectdiscovery/katana`
</Info>

Web crawler for endpoint and URL discovery.

| Input | Type | Description |
|-------|------|-------------|
| `targets` | Array | URLs or domains to crawl |
| `customFlags` | String | Additional CLI flags |

| Parameter | Type | Description |
|-----------|------|-------------|
| `depth` | Number | Maximum crawl depth (1–10, default: 3) |

---

### Wafw00f

WAF (Web Application Firewall) detection across target URLs.

| Input | Type | Description |
|-------|------|-------------|
| `targets` | Array | URLs to check for WAF |
| `customFlags` | String | Additional CLI flags |

| Parameter | Type | Description |
|-----------|------|-------------|
| `findAll` | Boolean | Detect all WAFs (not just the first match) |

---

## SAST & Code Analysis

### Semgrep

<Info>
[GitHub](https://github.com/semgrep/semgrep) · Docker: `semgrep/semgrep`
</Info>

Static Application Security Testing (SAST) with configurable rulesets.

| Input | Type | Description |
|-------|------|-------------|
| `target` | String | Source code content to scan |
| `customFlags` | String | Additional CLI flags |

| Parameter | Type | Description |
|-----------|------|-------------|
| `config` | String | Semgrep rule config (e.g., `auto`, `p/owasp-top-ten`, or custom YAML rules) |

---

## SSL/TLS Auditing

### TestSSL

<Info>
[GitHub](https://github.com/drwetter/testssl.sh) · Docker: `drwetter/testssl.sh`
</Info>

Comprehensive SSL/TLS configuration auditing.

| Input | Type | Description |
|-------|------|-------------|
| `target` | String | Hostname:port to audit (e.g., `example.com:443`) |
| `customFlags` | String | Additional CLI flags |

| Parameter | Type | Description |
|-----------|------|-------------|
| `protocols` | Boolean | Test SSL/TLS protocol support |
| `ciphers` | Boolean | Test cipher suites |

---

## Container Security

### Trivy

<Info>
[GitHub](https://github.com/aquasecurity/trivy) · Docker: `aquasec/trivy`
</Info>

Vulnerability scanner for container images, filesystems, and repositories.

| Input | Type | Description |
|-------|------|-------------|
| `target` | String | Container image name, filesystem path, or repo URL |
| `customFlags` | String | Additional CLI flags |

| Parameter | Type | Description |
|-----------|------|-------------|
| `scanType` | Select | `image`, `fs`, or `repo` |

---

## OSINT & Threat Intelligence

### theHarvester

<Info>
[GitHub](https://github.com/laramies/theHarvester) · Docker: `ghcr.io/laramies/theharvester`
</Info>

OSINT gathering for domains — emails, subdomains, hosts, and IPs from public sources.

| Input | Type | Description |
|-------|------|-------------|
| `domain` | String | Target domain to harvest |
| `customFlags` | String | Additional CLI flags |

| Parameter | Type | Description |
|-----------|------|-------------|
| `sources` | String | Comma-separated data sources (e.g., `baidu,bing,duckduckgo,yahoo`) |

---

### AbuseIPDB

IP address reputation lookup via the AbuseIPDB API.

| Input | Type | Description |
|-------|------|-------------|
| `ipAddress` | String | IPv4 or IPv6 address to check |
| `apiKey` | Secret | AbuseIPDB API key |

| Parameter | Type | Description |
|-----------|------|-------------|
| `maxAgeInDays` | Number | Max age for reports (default: 90) |
| `verbose` | Boolean | Include verbose information |

---

### VirusTotal

Multi-engine threat analysis for IPs, domains, file hashes, and URLs.

| Input | Type | Description |
|-------|------|-------------|
| `indicator` | String | IP, domain, file hash, or URL to inspect |
| `apiKey` | Secret | VirusTotal API key |

| Parameter | Type | Description |
|-----------|------|-------------|
| `type` | Select | `ip`, `domain`, `file`, or `url` |

---

## Malware Analysis

### YARA

<Info>
[GitHub](https://github.com/VirusTotal/yara) · Docker: `blacktop/yara`
</Info>

Pattern matching engine for malware classification using YARA rules.

| Input | Type | Description |
|-------|------|-------------|
| `target` | String | File content to scan |
| `rules` | String | YARA rule definitions |
| `customFlags` | String | Additional CLI flags |

---

## Notifications

### Notify

<Info>
[GitHub](https://github.com/projectdiscovery/notify) · Docker: `ghcr.io/zebbern/notify`
</Info>

Sends alerts to Slack, Discord, Telegram, or email.

| Input | Type | Description |
|-------|------|-------------|
| `messages` | Array | Alert messages to send |
| `providerConfig` | Secret | Provider configuration YAML |

| Parameter | Type | Description |
|-----------|------|-------------|
| `providerIds` | Multi-select | Restrict delivery to configured providers |
| `bulk` | Boolean | Send all messages as one payload |
| `silent` | Boolean | Suppress notify CLI output |

---

## Vulnerability Intelligence

### OSV

Query the OSV.dev API for known vulnerabilities affecting package versions.

| Input | Type | Description |
|-------|------|-------------|
| `packageSpecs` | Array | Package names with optional versions (for example `lodash@4.17.20`) |

| Parameter | Type | Description |
|-----------|------|-------------|
| `ecosystem` | Text | OSV ecosystem (`npm`, `PyPI`, `Go`, etc.) |
| `severityFloor` | Select | Minimum severity to include in results |
| `hydrateAdvisories` | Boolean | Fetch full advisory records for returned IDs |

---

### NVD

Query the NVD CVE API for vulnerability metadata and CVSS details.

| Input | Type | Description |
|-------|------|-------------|
| `cveIds` | Array | CVE identifiers (for example `CVE-2021-44228`) |

| Parameter | Type | Description |
|-----------|------|-------------|
| `keywordSearch` | Text | Optional keyword search when no CVE IDs are supplied |
| `resultsPerPage` | Number | Page size for NVD responses |

---

## Repository Helpers

### Repo Files Extractor

Fetches source and IaC files from a public GitHub repository for downstream scanners.

| Input | Type | Description |
|-------|------|-------------|
| `repositoryUrl` | String | Public GitHub repository URL |
| `ref` | String | Optional git ref (branch, tag, or commit) |

| Parameter | Type | Description |
|-----------|------|-------------|
| `ref` | Text | Default git ref when the input port is not connected |
| `maxFiles` | Number | Maximum number of files to extract |

---

### Manifest Extractor

Extracts dependency manifests from a public GitHub repository for CVE and SBOM workflows.

| Input | Type | Description |
|-------|------|-------------|
| `repositoryUrl` | String | Public GitHub repository URL |
| `manifestPaths` | Array | Optional explicit manifest paths |
| `includeDevDependencies` | Boolean | Include dev dependencies when parsing manifests |

| Parameter | Type | Description |
|-----------|------|-------------|
| `ref` | Text | Default git ref when the input port is not connected |
| `maxPackages` | Number | Maximum packages to emit per manifest |

---

## Cloud Integrations

### AWS MCP Group

Exposes curated AWS MCP servers (CloudTrail, IAM, CloudWatch, Lambda, DynamoDB, and more) to AI agent nodes.

| Input | Type | Description |
|-------|------|-------------|
| `credentials` | Contract | AWS credential bundle (`accessKeyId`, `secretAccessKey`, optional `sessionToken`) |

| Parameter | Type | Description |
|-----------|------|-------------|
| `enabledServers` | Multi-select | AWS MCP servers to register as tools |

---

## Example Workflow

A complete attack surface discovery pipeline:

```
Manual Trigger (domains input)
    ↓
Subfinder (subdomain discovery)
    ↓
DNSx (DNS resolution)
    ↓
httpx (HTTP probing)
    ↓
Nuclei (vulnerability scanning)
    ↓
Notify (notify team)
```
