# Agent: devops-ci

## Purpose

Improve the existing CI pipeline (`.github/workflows/ci.yml`) with Bun store caching and an API contract check step, and add a build validation job.

## Skills

Load before starting: devops-cicd

## Context

- CI workflow already exists at `.github/workflows/ci.yml` with three jobs: `lint`, `typecheck`, `test-unit`
- All three jobs use `oven-sh/setup-bun@v1` and `bun install --frozen-lockfile`
- Release workflow exists at `.github/workflows/release.yml` (Docker image builds)
- Root `package.json` scripts: `lint`, `typecheck`, `test`, `build` (not present — typecheck is `tsc --build`)
- OpenAPI spec lives at `workproject/openapi.json`
- Package manager: Bun (`bun@1.1.20` in `packageManager` field)
- `working-directory: workproject` is set on all jobs (monorepo subfolder)

## Subtasks

- [x] Add Bun store caching to the CI workflow — use `actions/cache` with `~/.bun/install/cache` path, keyed on `bun.lock` hash. Apply to all existing jobs (`lint`, `typecheck`, `test-unit`)
- [x] Add a `build` job to CI that runs `bun run typecheck` (which is `tsc --build`) to validate the full project builds. This can be combined with the existing `typecheck` job or kept separate if a `bun run build` script is added
- [x] Add an `api-contract` job that generates the OpenAPI spec from the running backend and diffs it against `openapi.json`. Steps: install deps → generate spec (check if a backend script exists, e.g. `bun --cwd=backend run openapi:generate`) → diff against `openapi.json` → fail if changes detected
- [x] If no OpenAPI generation script exists in `backend/package.json`, create one that extracts the spec from the NestJS Swagger module (e.g., `ts-node -e "..."` or a dedicated `scripts/generate-openapi.ts`)
- [x] Verify all jobs set `timeout-minutes` to reasonable values (lint: 10, typecheck: 10, test: 25, api-contract: 10)
- [x] Ensure `concurrency` group with `cancel-in-progress: true` remains on the workflow

## Notes

- The CI already exists and works. This is an improvement task, not a greenfield setup.
- Do NOT remove or break existing jobs — only add caching and the new `api-contract` job.
- Check `backend/package.json` for existing OpenAPI generation scripts before creating one.
- The `openapi.json` at root may be generated by a script like `scripts/generate-openapi.ts` — check before duplicating.

## Completion Summary

<!-- Appended by orchestrator after agent completes. Do not fill manually. -->
