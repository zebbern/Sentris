# Historical Drizzle Artifacts

This directory is non-authoritative and retained only as repository history.

Runtime migration commands, startup paths, and new generated migrations must use
[`backend/migrations`](../migrations). Do not point Drizzle configuration or
operational scripts back at this directory.
