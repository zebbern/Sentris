/**
 * Injection token for the Redis instance used by the MCP Registry module.
 * Separated from the module file to avoid circular imports.
 */
export const MCP_REGISTRY_REDIS = 'MCP_REGISTRY_REDIS';
