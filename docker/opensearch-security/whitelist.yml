# OpenSearch Security - API Whitelist (legacy name for allowlist)
#
# This file is required by securityadmin.sh even in OpenSearch 2.x.
# Actual configuration is in allowlist.yml.

---
_meta:
  type: 'whitelist'
  config_version: 2

config:
  enabled: false
  requests: {}
